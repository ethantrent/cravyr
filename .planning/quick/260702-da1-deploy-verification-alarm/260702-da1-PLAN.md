---
phase: 260702-da1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/server.ts
  - apps/api/src/__tests__/health.test.ts
  - .github/workflows/deploy-verify.yml
autonomous: true
requirements:
  - QUICK-260702-da1
must_haves:
  truths:
    - "GET /version returns the deployed commit SHA (RENDER_GIT_COMMIT, injected by Render)"
    - "A GitHub Action on push-to-main polls /version until it matches GITHUB_SHA and fails the run (triggering GitHub's failure email) if the deploy never lands"
    - "Test suite covers the /version endpoint"
  artifacts:
    - path: ".github/workflows/deploy-verify.yml"
      provides: "Post-push deploy verification with 20-minute polling window"
      contains: "GITHUB_SHA"
---

# Quick Task: Deploy-failure alarm (/version + post-push verification)

## Why

Render deploys failed silently from June 28 to July 2 (broken frozen-lockfile,
fixed in 652b7ff) — production served a stale build with no social routes and
nobody knew. Nothing in the repo verified that a push actually deployed.

## Tasks

1. Add `GET /version` to server.ts returning `RENDER_GIT_COMMIT`.
2. Add `deploy-verify.yml` workflow: poll /version for up to 20 minutes after
   each push to main; fail loudly on mismatch.
3. Test for /version; full suite green; atomic commit.
