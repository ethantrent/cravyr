---
phase: quick-260424-km4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/marketing/SUBMISSION-RUNBOOK.md
autonomous: true
requirements:
  - NOTIF-01
  - UX-01
  - UX-02
user_setup: []

must_haves:
  truths:
    - "Human has a single file to follow from credentials → App Store submission → Google Play submission with no gaps"
    - "Every one of the 9 human actions from STATE.md maps to a numbered runbook step with copy-pasteable commands"
    - "Every validation issue in app.config.ts is documented with file, line, current value, problem, and exact fix text"
    - "Runbook contains the 'Quick Sanity Command' at the top for regression-checking after any config change"
    - "Human knows the exact iPhone screenshot sizes required (6.7\" 1290×2796 and 5.5\" 1242×2208)"
    - "Each runbook step has acceptance criteria so the human knows when it is done"
    - "apps/mobile/app.config.ts is NOT modified (read-only validation pass only)"
  artifacts:
    - path: ".planning/marketing/SUBMISSION-RUNBOOK.md"
      provides: "Top-to-bottom App Store + Google Play submission runbook with embedded config validation findings"
      contains: "Config Validation Findings"
  key_links:
    - from: ".planning/marketing/SUBMISSION-RUNBOOK.md"
      to: ".planning/marketing/app-store-listing.md"
      via: "explicit reference in App Store Connect step for listing copy"
      pattern: "app-store-listing\\.md"
    - from: ".planning/marketing/SUBMISSION-RUNBOOK.md"
      to: "apps/mobile/app.config.ts"
      via: "Config Validation Findings section cites file:line for each issue"
      pattern: "apps/mobile/app\\.config\\.ts"
    - from: ".planning/marketing/SUBMISSION-RUNBOOK.md"
      to: "apps/mobile/eas.json"
      via: "ascAppId placeholder replacement step references eas.json:29"
      pattern: "eas\\.json"
---

<objective>
Produce a single executable submission runbook at `.planning/marketing/SUBMISSION-RUNBOOK.md` that consolidates the 9 human actions from STATE.md into an ordered, copy-pasteable checklist covering credentials → migrations → API deploy → config fixes → Apple Dev Portal → App Store Connect → EAS credentials → GitHub secrets → production build → screenshots → TestFlight → Google Play Console → submission. Embed a "Config Validation Findings" section listing every app.config.ts issue with exact file:line → replacement text.

Purpose: Eliminate the context-switching and guesswork that currently stands between "code complete" and "app live in the store." The human opens one file and executes top to bottom.

Output: `.planning/marketing/SUBMISSION-RUNBOOK.md` — single source of truth for submission; no edits to `apps/mobile/app.config.ts` in this task (validation is read-only).
</objective>

<execution_context>
@C:/Users/ethan/foodies/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ethan/foodies/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/marketing/app-store-listing.md
@apps/mobile/app.config.ts
@apps/mobile/eas.json
@apps/mobile/package.json
@apps/api/src/public/privacy.html

<validation_findings>
<!-- Pre-computed validation pass against apps/mobile/app.config.ts. Executor must -->
<!-- transcribe these into the runbook's "Config Validation Findings" section. -->
<!-- DO NOT modify app.config.ts during this task. -->

PASS checks (document as "verified OK" in runbook):
- ios.bundleIdentifier = 'com.cravyr.app' (line 19) — not default
- android.package = 'com.cravyr.app' (line 31) — matches iOS naming
- NSLocationWhenInUseUsageDescription (line 22-23) — specific, mentions "restaurants near you and show distance"
- expo-location locationWhenInUsePermission (line 47-48) — matches iOS string
- scheme = 'cravyr' (line 58) — Expo Router deep links working
- expo-notifications plugin icon + color = #f97316 (line 37-43) — verified
- android.adaptiveIcon.backgroundColor = '#f97316' (line 29) — set
- android.permissions (line 32) — only ACCESS_FINE_LOCATION / ACCESS_COARSE_LOCATION; no unused CAMERA/READ_EXTERNAL_STORAGE

FAIL / BLOCKER checks (document as ISSUE entries):

ISSUE 1 — version below 1.0.0
  File: apps/mobile/app.config.ts:7
  Current: `version: '0.0.1'`
  Problem: Apple and Google expect a production submission to be ≥ 1.0.0. Reviewers occasionally question sub-1.0 versions as "unfinished" and it locks you out of marketing copy that references "v1.0".
  Fix: `version: '1.0.0'`

ISSUE 2 — ios.buildNumber missing
  File: apps/mobile/app.config.ts (inside `ios: { ... }`, after bundleIdentifier around line 19-20)
  Current: (absent)
  Problem: EAS `autoIncrement: true` handles this for subsequent builds, but the first build needs an explicit baseline. Without it some CI paths log warnings.
  Fix: Add inside `ios: { ... }`:
    ```
    buildNumber: '1',
    ```

ISSUE 3 — android.versionCode missing
  File: apps/mobile/app.config.ts (inside `android: { ... }`, after package around line 31-32)
  Current: (absent)
  Problem: Same as ISSUE 2 — explicit baseline needed before autoIncrement takes over.
  Fix: Add inside `android: { ... }`:
    ```
    versionCode: 1,
    ```

ISSUE 4 — ITSAppUsesNonExemptEncryption missing (export compliance)
  File: apps/mobile/app.config.ts:21-24 (inside ios.infoPlist)
  Current: Only NSLocationWhenInUseUsageDescription declared
  Problem: Every App Store submission otherwise prompts "Does your app use encryption?" and blocks the build until answered. Declaring `false` (Cravyr only uses HTTPS which is exempt) avoids the prompt on every submission.
  Fix: Add inside `ios.infoPlist`:
    ```
    ITSAppUsesNonExemptEncryption: false,
    ```

ISSUE 5 — associatedDomains missing (deep links / AASA)
  File: apps/mobile/app.config.ts (inside `ios: { ... }`)
  Current: (absent)
  Problem: Privacy policy and future marketing links will deep-link into the app. Without `associatedDomains`, iOS cannot resolve universal links from a hosted `apple-app-site-association` file.
  Fix: Add inside `ios: { ... }` (only if a hosted AASA file at the domain is planned — otherwise note as "deferred to post-launch" in runbook):
    ```
    associatedDomains: ['applinks:cravyr.app'],
    ```
  Note in runbook: gated on actually publishing an AASA file at https://cravyr.app/.well-known/apple-app-site-association. If deferred, remove this entry to avoid a build warning.

ISSUE 6 — eas.json ascAppId placeholder
  File: apps/mobile/eas.json:29
  Current: `"ascAppId": "YOUR_APP_STORE_CONNECT_ID"`
  Problem: `eas submit` will fail. The real ID is the 10-digit numeric App Store Connect App ID from the app record.
  Fix: Replace with the numeric ID from App Store Connect → My Apps → Cravyr → App Information → Apple ID. Example: `"ascAppId": "6741234567"`.

ISSUE 7 — google-sa.json referenced but not present
  File: apps/mobile/eas.json:33
  Current: `"serviceAccountKeyPath": "./google-sa.json"`
  Problem: `eas submit --platform android` will fail until a Google Play service account JSON key is placed at `apps/mobile/google-sa.json` (and added to .gitignore). Generated from Google Cloud IAM with "Service Account User" role on the Play Console.
  Fix: Generate JSON key, save to `apps/mobile/google-sa.json`, confirm `apps/mobile/.gitignore` contains `google-sa.json`.
</validation_findings>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write SUBMISSION-RUNBOOK.md</name>
  <files>.planning/marketing/SUBMISSION-RUNBOOK.md</files>
  <action>
Create `.planning/marketing/SUBMISSION-RUNBOOK.md`. This is a single top-to-bottom executable checklist. DO NOT modify `apps/mobile/app.config.ts` — validation is read-only and goes in the runbook only.

Structure the file exactly as follows. Each numbered section must include: (a) a short "why this step exists" sentence, (b) copy-pasteable bash commands (forward slashes, Windows-compatible), (c) an "Acceptance" bullet list stating how the human knows the step is done.

# Cravyr Production Submission Runbook

Intro paragraph: one paragraph stating this is the only file the human needs to follow to get from code-complete to live in the App Store and Google Play. Reference STATE.md's 9 human actions as the source material.

## Quick Sanity Command

Place this ABOVE section 1. The human runs it after every config change to confirm expo's effective config is what they expect:

```bash
cd apps/mobile && npx expo config --type public | jq '{version, ios, android}'
```

## Config Validation Findings

Before the numbered sections. Start with a "PASS" sub-section listing every OK check from the validation_findings block above. Then for each of the 7 ISSUE entries from the validation_findings block, render as:

```
ISSUE N: <name>
File: apps/mobile/app.config.ts:<line>   (or apps/mobile/eas.json:<line>)
Current: <exact current text>
Problem: <why it's rejection / blocker risk>
Fix: <exact replacement text>
```

End this section with: "**Apply ISSUES 1–5 to `apps/mobile/app.config.ts` and ISSUE 6–7 to `apps/mobile/eas.json` at step 4 below. Re-run the Quick Sanity Command after each edit.**"

## 1. Pre-flight — Credentials & Accounts

Bullets for each:
- Apple Developer Program ($99/year) — https://developer.apple.com/programs/ — takes 24–48h for verification
- Google Play Console ($25 one-time) — https://play.google.com/console/signup
- Render paid tier ($7/month Starter) — required so `/privacy` URL stays available without cold starts
- Supabase project — already provisioned (see `.planning/STATE.md`)
- Google Cloud project with Places API enabled — already provisioned (Phase 02)
- Expo account — https://expo.dev signup, then `npx expo login`

Acceptance: `eas whoami` prints your Expo username; Apple Developer membership shows "Active"; Google Play Console shows developer account active; Render dashboard shows the cravyr-api service on Starter plan.

## 2. Apply Supabase Migrations

Commands (from repo root):
```bash
# Find migration files
ls apps/api/supabase/migrations/

# Apply via Supabase CLI (preferred)
cd apps/api && npx supabase db push

# OR via psql directly (fallback)
# SUPABASE_DB_URL is in .env.local or Supabase Dashboard → Project Settings → Database → Connection string (URI)
psql "$SUPABASE_DB_URL" -f apps/api/supabase/migrations/20260415000000_add_lat_lng_columns.sql
psql "$SUPABASE_DB_URL" -f apps/api/supabase/migrations/20260415100000_push_tokens.sql
```

Acceptance: `psql "$SUPABASE_DB_URL" -c "\d push_tokens"` shows the push_tokens table; `psql "$SUPABASE_DB_URL" -c "SELECT lat, lng FROM restaurants LIMIT 1"` does not error.

## 3. Deploy API to Render

Why: the privacy policy is hosted by the API at `https://cravyr-api.onrender.com/privacy`. App Store Connect requires a reachable URL before submission.

Commands:
```bash
# Render auto-deploys on push to main — trigger it:
git push origin main

# OR manually redeploy via Render dashboard → cravyr-api → Manual Deploy → Deploy latest commit
```

Acceptance:
```bash
curl -sS -o /dev/null -w "%{http_code}\n" https://cravyr-api.onrender.com/health
# → 200

curl -sS -o /dev/null -w "%{http_code}\n" https://cravyr-api.onrender.com/privacy
# → 200

curl -sS https://cravyr-api.onrender.com/privacy | grep -c "Privacy Policy"
# → ≥ 1
```

## 4. Fix Config Issues Flagged by Validation

Open `apps/mobile/app.config.ts` and apply ISSUES 1–5 from the Config Validation Findings section. Open `apps/mobile/eas.json` and apply ISSUES 6–7 (deferring ascAppId until step 6 when you have the real ID).

After edits, run:
```bash
cd apps/mobile && npx expo config --type public | jq '{version, ios, android}'
```

Acceptance: `version` is `"1.0.0"`; `ios.buildNumber` is `"1"`; `android.versionCode` is `1`; `ios.infoPlist.ITSAppUsesNonExemptEncryption` is `false`; `ios.infoPlist.NSLocationWhenInUseUsageDescription` matches the specific string.

## 5. Apple Developer Portal Setup

Steps (Apple Developer → Certificates, Identifiers & Profiles):
1. **Identifiers → App IDs → +** → Bundle ID: `com.cravyr.app` (exact match to app.config.ts:19)
2. Enable capabilities:
   - Push Notifications
   - Sign in with Apple
   - Associated Domains (only if ISSUE 5 was applied)
3. **Keys → +** → Create an APNs Authentication Key (Apple Push Notification service). Download the `.p8` file — you can only download once. Save it as `AuthKey_<KEY_ID>.p8` and note the Key ID and Team ID.

Acceptance: Bundle ID `com.cravyr.app` appears in Identifiers list with the three capabilities enabled; `.p8` key file exists locally; Key ID and Team ID noted.

## 6. App Store Connect — Create App Record

Steps (https://appstoreconnect.apple.com → My Apps → +):
1. Platform: iOS. Name: **Cravyr**. Primary Language: English (U.S.). Bundle ID: `com.cravyr.app`. SKU: `cravyr-ios-001`.
2. Open the app record → App Information → copy the **Apple ID** (numeric, ~10 digits). This is your `ascAppId`.
3. Update `apps/mobile/eas.json:29` by replacing `"YOUR_APP_STORE_CONNECT_ID"` with the real numeric ID.
4. Paste listing copy from `.planning/marketing/app-store-listing.md` into the relevant fields (name, subtitle, description, promotional text, keywords, URLs).
   - Privacy Policy URL: `https://cravyr-api.onrender.com/privacy`
   - Support URL: same or dedicated page
5. App Privacy section — declare data collected per the privacy policy:
   - Contact Info (email) — linked to identity
   - Location (coarse) — not linked, used for app functionality
   - Identifiers (device push token) — not linked, used for app functionality
   - Usage Data (swipes, saves) — linked, used for app functionality

Acceptance: App record visible in App Store Connect; `apps/mobile/eas.json` diff shows real 10-digit ascAppId; listing fields populated; App Privacy section shows "Complete."

## 7. EAS Credentials

Commands:
```bash
cd apps/mobile

# Interactive credentials flow (generates / uploads certificates & provisioning profiles)
eas credentials

# When prompted:
# - Platform: iOS
# - Profile: production
# - Let EAS generate Distribution Certificate and Provisioning Profile
# - Upload the APNs .p8 key from step 5 (Push Notifications → Add a Key)

# Repeat for Android:
eas credentials
# - Platform: Android
# - Profile: production
# - Let EAS generate a new Keystore (or upload existing) — SAVE THE BACKUP
```

Acceptance: `eas credentials` (non-interactive list) shows Distribution Certificate, Provisioning Profile, APNs Key for iOS, and a Keystore for Android.

## 8. GitHub Secrets

Steps (GitHub repo → Settings → Secrets and variables → Actions → New repository secret):
- `EXPO_TOKEN` — generate at https://expo.dev/settings/access-tokens and paste the value

Acceptance: Secret appears in the Actions secrets list (value hidden). The CI workflow at `.github/workflows/*.yml` references `secrets.EXPO_TOKEN` without a warning.

## 9. First Production Build

Commands:
```bash
cd apps/mobile

# Check expo-doctor first
npx expo-doctor

# Build both platforms in parallel
eas build --platform all --profile production
```

Wait 15–120 minutes (free tier queue). Monitor at https://expo.dev/accounts/<user>/projects/cravyr/builds.

Acceptance: Both iOS and Android builds show status "finished" in the EAS dashboard; download links produce a valid `.ipa` and `.aab`; build logs contain no red errors.

## 10. Screenshots

Required sizes (App Store Connect uploads):
- **iPhone 6.7" (required)** — 1290 × 2796 px — e.g., iPhone 16 Pro Max simulator
- **iPhone 5.5" (required, legacy)** — 1242 × 2208 px — e.g., iPhone 8 Plus simulator
- **iPad 12.9" (optional)** — 2048 × 2732 px — skip unless `ios.supportsTablet` is true (currently false, so skip)

Minimum 3 screenshots per size, maximum 10. Recommended scenes:
1. Discover screen with a swipe in progress (overlay label visible)
2. Tonight's Picks list with 3+ saves
3. Restaurant detail with photo, hours, directions button
4. Preferences screen
5. Auth screen (Apple Sign-In button visible)

Google Play also requires at least 2 screenshots, 1080 × 1920 px minimum.

Commands to capture from iOS Simulator:
```bash
# Launch a specific simulator, then:
xcrun simctl io booted screenshot /tmp/cravyr-discover.png
```

Acceptance: 3+ PNGs per required size; file dimensions verified with `file /tmp/cravyr-*.png` or `identify`; no personal data (real names, emails) visible in screenshots.

## 11. TestFlight Internal + External

Steps:
1. After the iOS build finishes, submit to App Store Connect:
   ```bash
   cd apps/mobile && eas submit --platform ios --profile production --latest
   ```
2. Wait ~15 minutes for Apple processing; build appears in App Store Connect → TestFlight.
3. Complete Export Compliance: answer "No" to non-exempt encryption (matches `ITSAppUsesNonExemptEncryption: false`).
4. Create an Internal Testing group → add yourself → install TestFlight on your device → run through full core loop.
5. Create an External Testing group → add 1–2 real testers (email addresses) → submit for Beta App Review (24–48h).

Acceptance: TestFlight app icon shows on your test device; at least one external tester completes onboard → swipe → save loop without guidance (per Phase 5 success criterion #4).

## 12. Google Play Console — Internal Track → Production

Steps:
1. Google Play Console → Create app → Cravyr → default language English (US) → category: Food & Drink
2. Submit Android build:
   ```bash
   cd apps/mobile && eas submit --platform android --profile production --latest
   ```
   (Requires `apps/mobile/google-sa.json` from ISSUE 7 fix.)
3. Fill Store Listing (short description, full description, screenshots from step 10, feature graphic 1024×500)
4. Fill Content Rating questionnaire (no user-generated content, no ads → likely Everyone / PEGI 3)
5. Fill Data safety section (match App Privacy from step 6)
6. Policy → Privacy Policy URL: `https://cravyr-api.onrender.com/privacy`
7. Push release from Internal testing → Closed testing → Production. Each promotion is a separate review cycle.

Acceptance: Internal testing release shows "Available on Play Store for testers"; you install via the opt-in link and complete the core loop.

## 13. Submit for Review + Rejection Remediation

Submit:
- iOS: App Store Connect → App Store tab → Create version 1.0.0 → attach the TestFlight build → fill "Sign in info" if Apple Sign-In testing requires a demo account → Submit for Review
- Android: Play Console → Production → Review & Roll out to Production

Common rejection reasons and fixes:

| Rejection | Cause | Fix |
|-----------|-------|-----|
| Guideline 4.2 — Minimum Functionality | App is "too simple" | Already mitigated: onboarding + detail view + saved list + preferences + settings all present (Phase 4) |
| Guideline 5.1.1(v) — Account Deletion | No in-app account deletion | Already mitigated: Settings → Delete Account (Phase 4, 04-05 plan) |
| Guideline 5.1.1 — Location permission string | Generic wording | Already mitigated: specific string references "restaurants near you and show distance" (app.config.ts:23) |
| Guideline 4.8 — Sign in with Apple | Offers Google sign-in but not Apple | Already mitigated: `usesAppleSignIn: true` + expo-apple-authentication (app.config.ts:20, 36) |
| Guideline 2.1 — App Completeness (crash on launch) | Build targets wrong architecture / missing env | Run TestFlight install on a physical device before submitting; verify EXPO_PUBLIC_API_URL points to production Render URL |
| Missing demo account | Auth-gated app | Add a test Apple ID credential in App Review Information → Sign-in info |
| Export compliance repeatedly asked | Missing ITSAppUsesNonExemptEncryption | Already mitigated by ISSUE 4 fix |

If rejected:
1. Read the full resolution center message
2. Fix the root cause (not just the symptom)
3. Reply to reviewer with a changelog of what was fixed
4. Resubmit — do NOT withdraw and re-create the version; it preserves review history

Acceptance: App Store Connect shows "In Review" then "Pending Developer Release" or "Ready for Sale"; Google Play shows "Published."

## Appendix A — 9 Human Actions from STATE.md → Runbook Step Mapping

| STATE.md Action | Runbook Step |
|-----------------|--------------|
| 1. Apply Supabase migrations | §2 |
| 2. Configure Apple Developer + Sign in with Apple | §5 |
| 3. Set ascAppId in eas.json | §6 (step 3) + ISSUE 6 |
| 4. Set EXPO_TOKEN GitHub secret | §8 |
| 5. Create and host privacy policy page | §3 (privacy already written at `apps/api/src/public/privacy.html`; this step deploys it) |
| 6. App icon 1024×1024 | Already complete (quick 260424-jw8); re-verify with `file apps/mobile/assets/icon.png` |
| 7. Run `eas build --platform all --profile production` | §9 |
| 8. Run `eas submit --platform ios` for TestFlight | §11 |
| 9. Capture App Store screenshots | §10 |

---
End of runbook.
  </action>
  <verify>
    <automated>test -f .planning/marketing/SUBMISSION-RUNBOOK.md && grep -c "^## " .planning/marketing/SUBMISSION-RUNBOOK.md | awk '{ if ($1 >= 14) print "OK: " $1 " sections"; else { print "FAIL: only " $1 " sections"; exit 1 } }' && grep -q "Quick Sanity Command" .planning/marketing/SUBMISSION-RUNBOOK.md && grep -q "Config Validation Findings" .planning/marketing/SUBMISSION-RUNBOOK.md && grep -q "ITSAppUsesNonExemptEncryption" .planning/marketing/SUBMISSION-RUNBOOK.md && grep -q "1290" .planning/marketing/SUBMISSION-RUNBOOK.md && grep -q "1242" .planning/marketing/SUBMISSION-RUNBOOK.md && grep -q "YOUR_APP_STORE_CONNECT_ID" .planning/marketing/SUBMISSION-RUNBOOK.md && grep -q "cravyr-api.onrender.com/privacy" .planning/marketing/SUBMISSION-RUNBOOK.md && ! git diff --name-only -- apps/mobile/app.config.ts | grep -q "app.config.ts" && echo "All checks passed"</automated>
  </verify>
  <done>
    - `.planning/marketing/SUBMISSION-RUNBOOK.md` exists with ≥14 `## ` sections (Quick Sanity, Config Validation Findings, §1–§13, Appendix A)
    - Contains the Quick Sanity Command at the top
    - Contains "Config Validation Findings" section with ≥7 ISSUE entries in the exact format (ISSUE N / File / Current / Problem / Fix)
    - Contains ITSAppUsesNonExemptEncryption reference (ISSUE 4)
    - Contains both iPhone screenshot sizes (1290 and 1242)
    - References the placeholder `YOUR_APP_STORE_CONNECT_ID` to be replaced
    - References `https://cravyr-api.onrender.com/privacy`
    - `apps/mobile/app.config.ts` has NOT been modified (git diff clean for that file)
  </done>
</task>

</tasks>

<verification>
1. Read the resulting `.planning/marketing/SUBMISSION-RUNBOOK.md` end-to-end and confirm a human could execute top-to-bottom without needing to open any other file except the explicitly referenced ones (`.planning/marketing/app-store-listing.md`, `apps/mobile/app.config.ts`, `apps/mobile/eas.json`, `apps/api/src/public/privacy.html`).
2. Confirm all 9 STATE.md human actions are mapped in Appendix A.
3. Confirm `git status apps/mobile/app.config.ts` shows no modifications (read-only validation constraint honored).
4. Confirm `git status apps/mobile/eas.json` shows no modifications (eas.json fix is queued as runbook instruction for the human, not applied by Claude in this task).
</verification>

<success_criteria>
- Single deliverable `.planning/marketing/SUBMISSION-RUNBOOK.md` committed
- All 9 STATE.md human actions traced to runbook steps in Appendix A
- Config Validation Findings section contains exactly the 7 ISSUE entries pre-computed in the planning context, each with file:line, current text, problem statement, and exact replacement text
- Quick Sanity Command is the first executable block in the file
- No edits to `apps/mobile/app.config.ts` or `apps/mobile/eas.json` in this task
- Runbook ≤ one file; no split into multiple documents
</success_criteria>

<output>
After completion, create `.planning/quick/260424-km4-generate-production-submission-runbook-a/260424-km4-SUMMARY.md`
</output>
