---
phase: 02-api-database-layer
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - apps/api/.env.example
autonomous: false
requirements:
  - API-01
user_setup:
  - service: google_places
    why: "All restaurant data flows through Google Places API"
    env_vars:
      - name: GOOGLE_PLACES_API_KEY
        source: "Google Cloud Console -> APIs & Services -> Credentials -> Create API Key -> Restrict to Places API (New)"
    dashboard_config:
      - task: "Enable Places API (New) in Google Cloud Console"
        location: "Google Cloud Console -> APIs & Services -> Library -> search 'Places API (New)' -> Enable"
      - task: "Create billing budget alert at $50/day"
        location: "Google Cloud Console -> Billing -> Budgets & alerts -> Create budget -> Set $50/day threshold with email notification"

must_haves:
  truths:
    - "GOOGLE_PLACES_API_KEY is documented in .env.example"
    - "TypeScript compilation passes for the complete API"
    - "Google Cloud billing alert is configured at $50/day (per D-11)"
    - "GOOGLE_PLACES_API_KEY env var is set and the API responds"
    - "PostGIS get_restaurant_recommendations function returns scored results after geo_cache migration"
  artifacts:
    - path: "apps/api/.env.example"
      provides: "Complete environment variable documentation including Google Places API key"
      contains: "GOOGLE_PLACES_API_KEY"
  key_links:
    - from: "apps/api/.env.example"
      to: "apps/api/src/services/places.ts"
      via: "GOOGLE_PLACES_API_KEY env var"
      pattern: "GOOGLE_PLACES_API_KEY"
    - from: "apps/api/src/routes/recommendations.ts"
      to: "supabase get_restaurant_recommendations RPC"
      via: "supabase.rpc('get_restaurant_recommendations')"
      pattern: "get_restaurant_recommendations"
---

<objective>
Finalize environment configuration, verify the full build, and guide the user through Google Cloud setup for billing alerts, API key configuration, and end-to-end validation including the recommendation engine.

Purpose: The in-app daily budget counter (D-12) is the actual cost protection, but the Google Cloud billing alert (D-11) is a safety net notification. This plan ensures the env var documentation is complete, the user has configured both the API key and the billing alert, and the PostGIS recommendation function still works correctly after the geo_cache migration added a geohash column to the restaurants table.

Output: Updated .env.example with GOOGLE_PLACES_API_KEY uncommented, verified typecheck, user-confirmed billing alert, recommendation endpoint validated.
</objective>

<execution_context>
@C:/Users/ethan/foodies/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ethan/foodies/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-api-database-layer/02-CONTEXT.md
@.planning/phases/02-api-database-layer/02-01-SUMMARY.md
@.planning/phases/02-api-database-layer/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update .env.example and verify full API build including recommendation function</name>
  <files>apps/api/.env.example</files>
  <read_first>
    apps/api/.env.example,
    apps/api/.env,
    apps/api/src/services/places.ts,
    apps/api/src/services/geo-cache.ts,
    apps/api/src/routes/restaurants.ts,
    apps/api/src/routes/recommendations.ts,
    apps/api/package.json
  </read_first>
  <action>
**Step 1:** Update `apps/api/.env.example` to uncomment and document the Google Places API key. The current file has `# GOOGLE_PLACES_API_KEY=your-key` commented out. Replace the full file content with:

```
# Cravyr API - Environment Variables
# Copy this file to .env and fill in your values

# Server
PORT=4000

# Supabase
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Google Places API (New) - REQUIRED for restaurant data
# Get from: Google Cloud Console -> APIs & Services -> Credentials
# Must enable "Places API (New)" in the API Library first
# Restrict key to Places API (New) only for security
GOOGLE_PLACES_API_KEY=your-google-places-api-key

# CORS (restrict in production)
# CORS_ORIGIN=*

# Environment
# NODE_ENV=development
```

**Step 2:** Run full typecheck to verify all new files compile together:
```bash
cd C:/Users/ethan/foodies && pnpm --filter cravyr-api exec tsc --noEmit
```

**Step 3:** Verify the PostGIS recommendation function is still intact after the geo_cache migration. The migration added a `geohash` column to the `restaurants` table, and the `get_restaurant_recommendations` function uses `SELECT r.*` which will now include this column. This is harmless but must be confirmed:
```bash
cd C:/Users/ethan/foodies && npx supabase db execute --sql "SELECT proname, pronargs FROM pg_proc WHERE proname = 'get_restaurant_recommendations';"
```
This should return one row confirming the function exists with 5 arguments.

**Step 4:** Verify the recommendation function actually executes without error (even with no data):
```bash
cd C:/Users/ethan/foodies && npx supabase db execute --sql "SELECT count(*) FROM get_restaurant_recommendations('00000000-0000-0000-0000-000000000000'::uuid, 40.7128, -74.0060);"
```
This should return 0 (no restaurants cached yet) without any SQL error. If it errors, the geohash column migration broke the function and must be investigated.
  </action>
  <verify>
    <automated>cd C:/Users/ethan/foodies && pnpm --filter cravyr-api exec tsc --noEmit && grep -c "GOOGLE_PLACES_API_KEY" apps/api/.env.example && npx supabase db execute --sql "SELECT count(*) FROM get_restaurant_recommendations('00000000-0000-0000-0000-000000000000'::uuid, 40.7128, -74.0060);"</automated>
  </verify>
  <acceptance_criteria>
    - apps/api/.env.example contains the line GOOGLE_PLACES_API_KEY=your-google-places-api-key (uncommented, not prefixed with #)
    - apps/api/.env.example contains comment "Must enable Places API (New)" in the API Library first"
    - apps/api/.env.example contains comment "Restrict key to Places API (New) only for security"
    - tsc --noEmit exits 0 for the full API package
    - grep for GOOGLE_PLACES_API_KEY in .env.example returns count >= 1
    - get_restaurant_recommendations function executes without SQL error after geo_cache migration
  </acceptance_criteria>
  <done>
    .env.example documents all required environment variables including GOOGLE_PLACES_API_KEY with setup instructions. Full TypeScript compilation passes for the API package with all new services and routes. PostGIS get_restaurant_recommendations function confirmed working after geohash column addition.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify Google Cloud billing alert, API key, and end-to-end functionality including recommendations</name>
  <files>apps/api/.env</files>
  <action>
This is a human verification checkpoint. The executor should present the verification steps below to the user and wait for confirmation. No automated code changes are needed -- the user must complete Google Cloud Console configuration and test the API endpoints manually.
  </action>
  <what-built>
Complete Phase 2 API + Database Layer:
- Google Places API v1 HTTP client with two-tier field masks (Pro for nearby, Enterprise for detail)
- Geohash-based geographic cluster cache (precision 5, ~5km cells) with two-layer caching (node-cache hot + Supabase cold)
- Three restaurant endpoints: GET /nearby (cached cluster fetch), GET /:id (lazy Enterprise detail), GET /:id/photos (photo reference resolution)
- Daily request budget counter (500/day cap, persisted in Supabase api_budget table)
- geo_cache table, api_budget table, upsert_restaurant RPC function in Supabase
- PostGIS get_restaurant_recommendations function verified intact after migration
  </what-built>
  <how-to-verify>
**Pre-requisites (do these first if not done):**
1. Go to Google Cloud Console -> APIs and Services -> Library
2. Search for "Places API (New)" and click Enable
3. Go to Credentials -> Create Credentials -> API Key
4. Restrict the key: Application restrictions = None (for now), API restrictions = Restrict to "Places API (New)" only
5. Copy the key and add it to `apps/api/.env` as `GOOGLE_PLACES_API_KEY=your-actual-key`

**Billing alert setup (D-11):**
6. Go to Google Cloud Console -> Billing -> Budgets and alerts
7. Create a new budget with $50/day threshold (or $1,500/month = $50/day * 30)
8. Add your email as the notification recipient
9. Confirm you see the budget listed in the Budgets and alerts page

**Functional verification:**
10. Start the API server: `cd apps/api && pnpm dev`
11. Test nearby endpoint: `curl "http://localhost:4000/api/v1/restaurants/nearby?lat=40.7128&lng=-74.0060"`
    - Expected: JSON response with `{ restaurants: [...], meta: { source: "google_places", count: N } }` where N > 0
12. Test the same request again (cache hit): `curl "http://localhost:4000/api/v1/restaurants/nearby?lat=40.7128&lng=-74.0060"`
    - Expected: `meta.source` should be `"memory"` (not `"google_places"`)
13. Pick a restaurant ID from the response and test detail: `curl "http://localhost:4000/api/v1/restaurants/{id}"`
    - Expected: JSON with restaurant data including rating (lazy-fetched from Google)
14. Test photos: `curl "http://localhost:4000/api/v1/restaurants/{id}/photos"`
    - Expected: `{ photos: ["https://lh3.googleusercontent.com/...", ...] }`
15. Test input validation: `curl "http://localhost:4000/api/v1/restaurants/nearby?lat=999&lng=0"`
    - Expected: 400 error with "lat must be -90 to 90"

**Recommendation endpoint verification (Success Criterion 4):**
16. Test the recommendation function via the API (requires auth token -- use a test user):
    `curl -H "Authorization: Bearer YOUR_TOKEN" "http://localhost:4000/api/v1/recommendations?lat=40.7128&lng=-74.0060"`
    - Expected: JSON array of restaurants scored by distance/cuisine/rating/price, ordered by rank descending
    - If no restaurants are cached yet for this area, run step 11 first to populate the cache, then retry
    - The response should include the `geohash` field on each restaurant (confirming the migration did not break the function's `SELECT r.*`)
  </how-to-verify>
  <verify>
    <automated>cd C:/Users/ethan/foodies && grep -c "GOOGLE_PLACES_API_KEY=" apps/api/.env</automated>
  </verify>
  <acceptance_criteria>
    - User confirms Google Cloud billing alert is set at $50/day
    - User confirms GOOGLE_PLACES_API_KEY is set in apps/api/.env
    - User confirms GET /restaurants/nearby returns restaurants on first call (source: google_places)
    - User confirms GET /restaurants/nearby returns cached data on second call (source: memory)
    - User confirms GET /restaurants/:id returns detail with rating field populated
    - User confirms GET /restaurants/:id/photos returns photo URLs array
    - User confirms GET /restaurants/nearby?lat=999 returns 400 error
    - User confirms GET /recommendations?lat=XX&lng=YY returns scored restaurant array (or empty array if no cached data in area)
  </acceptance_criteria>
  <done>
    Google Cloud billing alert configured at $50/day. GOOGLE_PLACES_API_KEY env var set. All three restaurant endpoints verified: nearby returns cached cluster data, detail lazily fetches Enterprise fields, photos resolves references to fresh URLs. Input validation rejects out-of-range coordinates. Recommendation endpoint returns scored results from PostGIS function, confirming the geo_cache migration did not break the existing recommendation SQL function.
  </done>
  <resume-signal>Type "approved" if all verification steps pass, or describe any issues encountered.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User -> Google Cloud Console | Manual billing alert configuration |
| User -> .env file | API key stored in server-side environment |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | Information Disclosure | .env file | mitigate | .env is in .gitignore (verified in Phase 1); .env.example contains placeholder values only. API key never committed to git. |
| T-02-03-02 | Denial of Service | Google Places billing | mitigate | D-11 billing alert at $50/day as notification safety net; D-12 in-app daily counter at 500 requests as actual cap; API key restricted to Places API (New) only (prevents abuse via other Google APIs). |
</threat_model>

<verification>
1. .env.example has GOOGLE_PLACES_API_KEY documented and uncommented
2. tsc --noEmit passes for the full API
3. get_restaurant_recommendations SQL function executes without error post-migration
4. User confirms billing alert is configured
5. User confirms nearby endpoint returns cached restaurants on second call
6. User confirms recommendation endpoint returns scored results
</verification>

<success_criteria>
Phase 2 is complete: the Express API correctly proxies Google Places with field masks enforced, geographic cluster caching is active (confirmed by memory/database source on repeat calls), the detail endpoint lazily fetches Enterprise fields, photos are resolved to fresh URLs, the daily budget counter prevents cost overruns, the Google Cloud billing alert is configured as a safety net, and the PostGIS recommendation function returns scored results correctly after the geo_cache migration.
</success_criteria>

<output>
After completion, create `.planning/phases/02-api-database-layer/02-03-SUMMARY.md`
</output>
