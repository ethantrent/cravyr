node_modules/
.expo/
dist/
packages/shared/src/**/*.js
packages/shared/**/*.tsbuildinfo
.turbo/
*.log
.env
.env.local
.env.*.local

# Play Store submit key (local only; path referenced in eas.json)
apps/mobile/google-sa.json

# Claude Code local config (project-level, not for version control)
.claude/

# Expo dynamic config stub (app.config.ts is authoritative)
app.json
